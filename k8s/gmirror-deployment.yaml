apiVersion: apps/v1
kind: Deployment
metadata:
  name: gmirror
  namespace: gstack
  labels:
    app: gmirror
    component: gstack
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gmirror
  template:
    metadata:
      labels:
        app: gmirror
        component: gstack
    spec:
      containers:
      - name: gmirror
        image: ghcr.io/gstack/gmirror:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3002
          name: http
        envFrom:
        - configMapRef:
            name: gstack-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 3002
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: /data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: gmirror-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: gmirror-svc
  namespace: gstack
  labels:
    app: gmirror
    component: gstack
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
    name: http
  selector:
    app: gmirror
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gmirror-pvc
  namespace: gstack
  labels:
    app: gmirror
    component: gstack
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
